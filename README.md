# Public Service React

## Installation

Use node v8.17.0 (npm v6.13.4)

1. `npm i`
2. Create file `public-service-react/server/config/keys.dev.js` with `mongoUrl` and `jwt` variables.
3. `npm start`
